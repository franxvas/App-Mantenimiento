# Plantillas Excel

Coloca aquí las plantillas `*_Base_ES.xlsx` y `*_Reportes_ES.xlsx` requeridas por
la función `initSchemasFromTemplates`. Estas plantillas se gestionan fuera del
repositorio y no se versionan en Git.
