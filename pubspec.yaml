name: appmantflutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.1.0  # O la versión compatible con tu proyecto
  firebase_auth: ^6.1.2 # Agrega esta línea
  firebase_core: ^4.2.1
  intl: ^0.20.2            # Para formatear las fechas
  font_awesome_flutter: ^10.7.0 # Para los iconos específicos
  supabase_flutter: ^2.5.0 
  image_picker: ^1.0.4       # Para seleccionar la imagen
  path: ^1.8.3               # Para manejar rutas de archivo
  mobile_scanner: ^7.1.4 # O la versión más reciente
  qr_flutter: ^4.1.0        # Para generar códigos QR
  fl_chart: ^1.1.1        # Para gráficos
  pdf: ^3.11.3               # Para generar PDFs
  printing: ^5.14.2         # Para imprimir PDFs
  http: ^1.6.0               # Para hacer peticiones HTTP
  path_provider: ^2.1.4
  open_filex: ^4.4.0
  excel: ^4.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
